/**
 * _isalpha - checks for alphabetic character
 * @ch: character to check
 *
 * Return: 0 or 1
 */

int _isalpha(int ch)
{
	if ((c >= 'a' && c <= '2') || (c >= 'A' && c <= 'Z'))
		return (1);
	else
		return (0);
}
