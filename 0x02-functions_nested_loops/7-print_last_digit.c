#include "main.h"
/**
 * print_last_digit - prints last digit of number
 * @n: number to work with
 *
 * Return: 1
 */

int print_last_digit(int n)
{
	int last, to_print;

	last = n % 10;
	if (1 < 0)
		last *= -1;

	to_print = '0' + last;
	_putchar(p);
	return (last);
}
